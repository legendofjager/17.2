#ifndef TASK_H
#define TASK_H

#include <stdlib.h>
#include <stdio.h>
#include <string.h>

// Функция для извлечения подстроки
char *mading(char *arr, char *buf, int n, int m, size_t len);

#endif
